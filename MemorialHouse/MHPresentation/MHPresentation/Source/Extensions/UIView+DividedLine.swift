import UIKit

extension UIView {
    static func dividedLine() -> UIView {
        let line = UIView()
        line.backgroundColor = .dividedLine
        line.setHeight(1)
        
        return line
    }
}
