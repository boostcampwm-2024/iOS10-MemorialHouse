public struct BookCategory {
    let order: Int
    let name: String
    
    public init(
        order: Int,
        name: String
    ) {
        self.order = order
        self.name = name
    }
}
